import { BaseCustomWebComponentConstructorAppend, css, html } from "@node-projects/base-custom-webcomponent";
export class MermaidFlowchartDirective extends BaseCustomWebComponentConstructorAppend {
    static style = css `
    :host {
        background: #fffbe6;
        border: 1px solid #d6b656;
        color: #6b5600;
        display: block;
        font-family: Arial, sans-serif;
        font-size: 11px;
        overflow: hidden;
        padding: 4px 6px;
        text-overflow: ellipsis;
        white-space: nowrap;
    }
    `;
    static template = html `<span id="text"></span>`;
    static is = "mermaid-flowchart-directive";
    line = "";
    _text;
    static properties = {
        line: String,
    };
    constructor() {
        super();
        this._restoreCachedInititalValues();
        this._text = this._getDomElement("text");
    }
    async ready() {
        this._parseAttributesToProperties();
        this._render();
    }
    attributeChangedCallback(name, oldValue, newValue) {
        if (oldValue !== newValue) {
            this._parseAttributesToProperties();
            this._render();
        }
    }
    _render() {
        this._text.textContent = this.getAttribute("line") ?? this.line;
    }
    createMermaid() {
        return this.getAttribute("line") ?? this.line;
    }
}
customElements.define(MermaidFlowchartDirective.is, MermaidFlowchartDirective);
